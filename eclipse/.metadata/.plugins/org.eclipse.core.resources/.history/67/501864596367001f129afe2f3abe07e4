package Basil.module.movement;

import java.util.ArrayList;

import org.lwjgl.input.Keyboard;

import Basil.Client;
import Basil.module.Category;
import Basil.module.Module;
import de.Hero.settings.Setting;

public class TNTWarp extends Basil.module.Module{
	
	public TNTWarp() {
		super("TNTwarp", -1, Category.MOVEMENT);
	}
	
	public void setup() {
		Client.sm.rSetting(new Setting("Horizontal", this, 1.5, 1, 2.5, false));
		Client.sm.rSetting(new Setting("Vertical", this, 1.5, 1, 2.5, false));
	}
	
	public void onUpdate() {
		if (isToggled()) {
			if (mc.thePlayer.hurtTime == 8) {
				
			}
		}
	}
}
